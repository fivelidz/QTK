# If you want to fund QTK directly, the upstream project RTK
# (rtk-ai/rtk) is the one that does the real heavy lifting and serves
# 13 different AI coding agents — consider funding them instead:
# https://github.com/sponsors/rtk-ai (if/when they enable Sponsors).
#
# QTK doesn't currently accept funding. This file exists to redirect
# any incidental support to the upstream RTK project rather than absorb
# it into a single-agent variant.
